/*
 *    Date: 
 *  Author: 
 * Project: 
 *
 * 
 */

var entTemp = require(process.cwd()+"/../lib/entityTemplate").entityTemplate;
var util = require("util");
var self = {};

var movePhoto = function()
{
  movePhoto.super_.call(this);

  this.moduleName = "Entity movePhoto";
  this.attachTableName = "move_photo";
}

/*
 * Inheritance
 */
util.inherits(movePhoto,entTemp);

movePhoto.prototype.init = function(opt)
{
  self = this;
  movePhoto.super_.prototype.init.call(self,opt);
}

/*
 * New methods
 */

/*
 * Export
 */
exports.movePhoto = movePhoto;

/*
 * Private function
 */
