/*
 *    Date: 
 *  Author: 
 * Project: 
 *
 * 
 */

var entTemp = require("../../lib/entityTemplate").entityTemplate;
var util = require("util");
var self = {};

var wgMapRS = function(schema)
{
  wgMapRS.super_.call(this);

  this.moduleName = "Entity wgMapRS";
  this.entityName = "wgMapRS";
  this.schemaName = schema;
  this.tableName = "wg_map_rs";

  this.entityId = "id";
  this.entityIdType = this.crudUtils.INTEGER;
}

/*
 * Inheritance.
 */
util.inherits(wgMapRS,entTemp);

wgMapRS.prototype.init = function(opt)
{
  self = this;
  wgMapRS.super_.prototype.init.call(self,opt);
}

/*
 * Export.
 */
exports.wgMapRS = wgMapRS;
